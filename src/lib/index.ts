/**
 * Barrel export — re-exports types and utilities for convenient imports.
 *
 * Components should import from `$lib` for types and utils,
 * and from `$lib/services/*` for backend calls.
 */
export * from "./types";
export * from "./utils";
